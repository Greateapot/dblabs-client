import 'package:flutter/material.dart';

class Lab4Page extends StatelessWidget {
  const Lab4Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Пустовато"),
    );
  }
}
